publisher = client.publish('/chats', {
  message: '<%= j render @chat %>'
});

publisher.callback(function() {
  $('#chat_body').val('');
  $('#new_chat').find("input[type='submit']").val('send').prop('disabled', false)
});

publisher.errback(function() {
  alert('There was an error while sending your message');
});